{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "test": "pulp build -I test",
    "build": "pulp build"
  },
  "devDependencies": {
    "bower": "^1.8.4",
    "pulp": "^12.3.0",
    "purescript": "^0.12.1",
    "rimraf": "^2.6.2"
  }
}
