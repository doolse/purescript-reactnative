{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "test": "pulp build -I test",
    "build": "pulp build"
  },
  "devDependencies": {
    "purescript": "^0.12.0",
    "rimraf": "^2.4.1"
  }
}
